<br>
<%= link_to 'Main page', root_path %>
<br>
<hr>
<%= link_to 'new category', new_category_path %>
<hr>

<h3>categories</h3>
<table class="table">
  <thead>
  <tr >
    <th class="col-lg-1"><%= link_to "Id", :sort => "id"%> </th>
    <th class="col-lg-1"><%= link_to "Category Title", :sort => "category_name"%> </th>
    <th class="col-lg-1 text-right"><%= link_to "Created at", :sort => "created_at"%> </th>
  </tr>
  <% @categories.each do |category| %>
    <tr>
      <td><%= category.id %></td>
      <td><%= link_to "#{category.title}", category_path(category.id) %></td><td>
      <td><%= category.created_at %></td>
    </tr>
  <% end %>
  </thead>
</table>
<hr>
